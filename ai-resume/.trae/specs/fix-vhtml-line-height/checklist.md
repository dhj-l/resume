# Checklist

- [x] `ResumePreview.vue` 的 `.resume-preview-wrapper` 添加了 `:style="{ lineHeight: globalLineHeight }"` 绑定
- [x] `ResumePreview.vue` 正确导入并使用了 `globalLineHeight` 计算属性
- [x] `ProjectExperienceSection.ts` 移除了 `leading-relaxed` 类
- [x] `InternshipExperienceSection.ts` 移除了 `leading-relaxed` 类
- [x] `WorkExperienceSection.ts` 移除了 `leading-relaxed` 类
- [x] `CampusExperienceSection.ts` 移除了 `leading-relaxed` 类
- [x] `SelfEvaluationSection.ts` 移除了 `leading-relaxed` 类
- [x] 项目构建成功，无 TypeScript 或编译错误
- [x] CSS 样式修改符合项目代码规范
