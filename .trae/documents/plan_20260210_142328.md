I will implement the `TemplateDetailsPage` with the following steps:

1.  **Update API Types**: Modify `src/api/templates/type.ts` to include the `TemplateDetails` interface that matches your provided JSON structure (handling nested `resume` and `user` objects).
2.  **Add API Service**: Update `src/api/templates/templates.ts` to add the `getTemplateByIdAPI` function.
3.  **Implement TemplateDetailsPage**:
    -   Fetch template details using the new API.
    -   Create a responsive layout (consistent with Home style) using Tailwind CSS.
    -   **Left Side**: Large preview image with shadow and rounded corners.
    -   **Right Side**: Template information including Title, Category badge, Usage count, Author info, and dates.
    -   **Action**: Add a "Use Template" button that (for now) can redirect to the editor or show a success message.
    -   Add loading states and error handling.
    -   Ensure type safety using the defined interfaces.

This will complete the development of the page as requested, matching the style of the `home` views.