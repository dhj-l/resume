I will implement the complete flow from Template List to Resume Export.

### 1. Update API Definitions
**File:** `src/api/resume/resume.ts`
- Update `createResumeAPI` to accept an optional payload `{ templateId?: string }`.

### 2. Update Resume Store
**File:** `src/stores/resumeStore.ts`
- Update `createResume` action to accept `templateId` and pass it to the API.
- Refactor `saveResume` action to use `resumeData.value._id` directly instead of requiring an external `id` argument, ensuring we always update the current resume.

### 3. Implement Template Usage Logic
**File:** `src/views/template/TemplateDetailsPage.vue`
- Update `handleUseTemplate` to:
  1. Call `resumeStore.createResume` with the template ID.
  2. Retrieve the new resume ID.
  3. Navigate to the editor with the ID: `/editor?id=${newId}`.

### 4. Initialize Editor with Resume Data
**File:** `src/views/editor/EditorPage.vue`
- Add `onMounted` logic to check for `id` in the URL query parameters.
- If an ID exists, call `resumeStore.getResumeDetail` to load the correct resume data instead of using mock data.

### 5. Fix Editor Header Saving
**File:** `src/views/editor/components/EditorHeader.vue`
- Remove the hardcoded resume ID in `saveResumeWithCover`.
- Update the call to `saveResume` to rely on the store's internal state (as refactored in step 2).

### 6. Verification
- Verify the flow: Template List -> Click Template -> Details Page -> Click "Use Template" -> Editor (Data Loaded) -> Save -> Export.
