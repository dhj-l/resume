# Checklist

- [x] `SortableModule` 接口已创建，包含 `content` 和 `globalSort` 字段
- [x] `skills` 字段类型已从 `string` 改为对象类型
- [x] `certificates` 字段类型已从 `string` 改为对象类型
- [x] `selfEvaluation` 字段类型已从 `string` 改为对象类型
- [x] `InternshipExperience` 接口已添加 `globalSort` 和 `localSort` 字段
- [x] `ProjectExperience` 接口已添加 `globalSort` 和 `localSort` 字段
- [x] `CampusExperience` 接口已添加 `globalSort` 和 `localSort` 字段
- [x] `WorkExperience` 接口已添加 `globalSort` 和 `localSort` 字段
- [x] `EducationBackground` 接口已添加 `globalSort` 和 `localSort` 字段
- [x] `BasicInfo` 接口已添加 `globalSort` 字段
- [x] `JobIntention` 接口已添加 `globalSort` 字段
- [x] 所有排序字段已添加 JSDoc 注释
- [x] TypeScript 类型检查通过，无编译错误
